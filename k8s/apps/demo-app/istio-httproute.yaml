apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: demo-route
  namespace: demo
spec:
  parentRefs:
    - name: demo-gateway
  hostnames:
    - demo-istio.local
  rules:
    - backendRefs:
        - name: demo-app
          port: 80
